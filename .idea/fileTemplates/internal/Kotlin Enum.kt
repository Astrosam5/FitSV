#parse("File Header.java")
#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME}
#end

/**
 * Enum ${NAME}
 * TODO Comment use case of enum ${NAME}
 * @author Samuel Albareda Zumelzu
 * @author Valentin Ayroles
 */
enum class ${NAME} {
}